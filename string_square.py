# Q.write a program that can read the integer t . Then for t lines read another
# interger N and for each N value print a N*N square filled with the character
# '#'.


t = int(input('Testcase : '))
for i in range(t):
    n= int(input('N : '))
    for r in range(n):
        print(' SORRY '*n)